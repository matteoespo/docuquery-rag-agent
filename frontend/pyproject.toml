[project]
name = "docuquery-frontend"
version = "0.1.0"
description = "DocuQuery RAG Agent Frontend"
requires-python = ">=3.12"
dependencies = [
    "streamlit==1.56.0",
    "requests==2.33.1",
    "python-dotenv>=1.0.1,<2.0.0",
    "langsmith>=0.1.98,<0.2.0",
]

[tool.setuptools]
packages = ["api", "components", "pages", "utils"]
